(* => Mandatory)
SERVER_PORT=                    // 3000
SERVER_JWT_SECRET=              // TopseyCret
SERVER_JWT_EXPIRATION_TIME=     // 3600

TYPEORM_USERNAME=               * // get value from docker/.env DB_ROOT_USER
TYPEORM_PASSWORD=               * // get value from docker/.env DB_ROOT_PASS
TYPEORM_DATABASE=               * // get value from docker/.env DB_INIT_DB
TYPEORM_HOST=                   // localhost
TYPEORM_PORT=                   // 5432
TYPEORM_SYNCHRONIZE=            // false
TYPEORM_DROP_SCHEMA=            // false
TYPEORM_RUN_MIGRATION=          // true
TYPEORM_LOG=                    // false

NETWORK_ID                      // 4 (Rinkeby Network Id)
WALLET_PRIVATE_KEY              *
WALLET_PUBLIC_KEY               *
INFURA_KEY                      *